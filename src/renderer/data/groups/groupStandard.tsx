import packM15Regular from '../packs/packM15Regular';
import { TemplateGroup } from '../../types/TemplateGroup';

const groupStandard: TemplateGroup = {
  packs: [{ name: 'M15 Regular Pack', pack: packM15Regular }],
};

export default groupStandard;
